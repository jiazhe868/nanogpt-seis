# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
.DS_Store
.claude/
2026-*-i-want-to-build-and-train-a-nano-gpt-for-earthqua.txt

# --- Large data (regenerate with the crawlers) ---
data/raw/
data/tokenized/
data/processed/*.jsonl
data/processed/*.log
# keep the corpus stats for reference
!data/processed/stats.json

# --- Model weights (too big for git; use releases / LFS if needed) ---
checkpoints/*.pt
checkpoints/**/*.pt
checkpoints/**/*.pth
checkpoints/**/*.ckpt
checkpoints/**/*.safetensors
checkpoints/scaling/
# keep small logs & plots so the training curves are reproducible/inspectable
!checkpoints/*.csv
!checkpoints/*.log
!checkpoints/*.png

# generated figures are committed (assets/*.png) so the README renders on GitHub

# editor swap files
*.swp
*.swo
*~
