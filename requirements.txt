# Tested with (Python 3.11, conda env `nanogpt_seis`):
#   torch 2.6.0+cu124 · tokenizers 0.23.1 · datasets 5.0.0 · datasketch 1.10.0
#   pymupdf 1.28.0 · requests 2.34.2 · beautifulsoup4 4.15.0 · feedparser 6.0.12
#   numpy 1.26.4 · pyyaml 6.0.3 · matplotlib 3.11.0 · graphviz 0.21

# --- crawling / processing ---
requests
beautifulsoup4
feedparser
pymupdf            # PDF text extraction (imported as `fitz`)
datasketch         # MinHash near-dup removal
datasets           # streaming Wikipedia / FineWeb-Edu general-text mix
tqdm

# --- tokenizer ---
tokenizers         # HuggingFace byte-level BPE trainer

# --- model / training ---
# torch is provided by the conda env (2.6.0+cu124, matches driver CUDA 12.5).
numpy
pyyaml
wandb              # optional logging; training also writes plain CSV

# --- figures ---
matplotlib
graphviz           # workflow/architecture diagrams (needs the system `dot` binary:
                   #   conda install -c conda-forge graphviz python-graphviz)
